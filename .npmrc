# Desliga o "deps status check" que o pnpm 11+ roda antes de cada script.
# Esse check trava quando há ignored build scripts (sharp/etc.) — pra POC interna não vale o boilerplate.
verify-deps-before-run=false
